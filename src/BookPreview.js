import React from 'react';

const Bookpreview = (props) => {
    const { title,author,isbn,publication,genre,sold,language,image} = props
    return (
        <div>
            
        </div>
    );
}

export default Bookpreview;
